An ETag lets the client validate a stored response. The server supplies it with the JSON, and the client sends that value in If-None-Match on a later conditional GET. For an otherwise successful GET, a tag matching the selected representation leads to 304 Not Modified with no response body, so the client can reuse its stored body. If the tag does not match, a successful GET returns 200 OK with the current JSON.

Validation still sends a request. To allow reuse without validation, set an appropriate freshness policy, for example Cache-Control: max-age=60. Subject to other caching rules, the cached response can be reused while fresh without contacting the origin. That permission does not guarantee that the origin data will remain unchanged for 60 seconds.