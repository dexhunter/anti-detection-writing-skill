You are an expert AI-text detector. Classify the passage below.
0 = no AI involvement (entirely human-written)
1 = lightly AI-edited
2 = moderately AI-edited
3 = heavily AI-edited or fully AI-generated
Respond with a single digit (0, 1, 2, or 3) and nothing else.

Passage:
"""
an etag lets the client validate a stored response, but validation still sends a request. the server supplies the etag with the json; the client includes that value in if-none-match on a later conditional get. if the tag matches the selected representation, an otherwise successful get returns 304 not modified with no response body, allowing reuse of the stored body. if it does not match, a successful get returns 200 ok with the current json. to allow reuse without validation, set an appropriate freshness policy, such as cache-control: max-age=60, subject to the other caching rules. the client can reuse the cached response while fresh; this does not guarantee unchanged underlying data for 60 seconds.
"""

Answer: 