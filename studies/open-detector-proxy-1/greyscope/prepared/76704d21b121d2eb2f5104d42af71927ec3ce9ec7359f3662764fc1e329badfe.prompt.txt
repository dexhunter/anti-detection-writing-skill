You are an expert AI-text detector. Classify the passage below.
0 = no AI involvement (entirely human-written)
1 = lightly AI-edited
2 = moderately AI-edited
3 = heavily AI-edited or fully AI-generated
Respond with a single digit (0, 1, 2, or 3) and nothing else.

Passage:
"""
etag enables receiving applications to validate whether cached representations may be reused. still, this validation includes sending a request. a server sends an etag with the json response and the client includes that tag value in the if-none-match header for future conditional get requests. for an otherwise successful get, if the cached etag matches the selected representation's tag, the server returns 304 not modified with no response body and the client can reuse its cached body. if the etag does not match, the server returns 200 ok with the current json if the get otherwise succeeds. an application may employ a freshness policy such as cache-control: max-age=60, in which case the cached response may be reused while fresh without a validation request, subject to other caching rules. however, reuse under this freshness policy is not a guarantee that the underlying data will remain unchanged for 60 seconds.
"""

Answer: 