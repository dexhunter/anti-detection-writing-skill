You are an expert AI-text detector. Classify the passage below.
0 = no AI involvement (entirely human-written)
1 = lightly AI-edited
2 = moderately AI-edited
3 = heavily AI-edited or fully AI-generated
Respond with a single digit (0, 1, 2, or 3) and nothing else.

Passage:
"""
> and further if i override the settings for a specific chat do those take precedence? a per-chat override takes precedence for supported generation parameters such as temperature. if the chat leaves a parameter unset, open webui uses your user-level default, then the selected model's setting in the admin panel. ollama or llama-server supplies its default when the parameter is omitted from the request. so a model setting of temperature=0.2 and a chat override of 0.8 sends 0.8. changing these settings affects api requests; it doesn't rewrite the ollama modelfile or llama-server startup arguments. you can see the two merges in the source: the [frontend puts chat values over user settings](https://github.com/open-webui/open-webui/blob/0a7c15832fb30b1903753e83f81dc7d27e5b0944/src/lib/components/chat/chat.svelte#l3554-l3558), then the [backend fills model defaults where the request lacks a value](https://github.com/open-webui/open-webui/blob/v0.11.3/backend/open_webui/utils/payload.py#l69-l82). for the odd behavior you're seeing, compare the outgoing request with the server's supported api fields. system prompts, streaming controls, and server-only options have separate handling, so this precedence isn't a rule for every control. a ui field cannot override an option that the server only accepts at startup.
"""

Answer: 