An ETag lets the client check whether it can reuse a stored response. The server sends the ETag with the JSON; on a later conditional GET, the client sends that value in If-None-Match. If the tag matches the selected representation, an otherwise successful GET returns 304 Not Modified with no response body, and the client reuses its stored body. If the tag does not match, a successful GET returns 200 OK with the current JSON.

Validation still requires a request. To allow reuse without validation, set an appropriate freshness policy, such as Cache-Control: max-age=60. Subject to the other caching rules, the client can reuse the response while it is fresh. This does not guarantee that the underlying data stays unchanged for 60 seconds.


