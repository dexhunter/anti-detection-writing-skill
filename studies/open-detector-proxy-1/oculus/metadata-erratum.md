The frozen model-manifest.json adaptation note incorrectly mentions a publisher formatting typo. The pinned raw README uses valid `{prob:.4f}` formatting; an earlier web-rendered extraction introduced the apparent space. The missing-v repository identifier is a real typo. Neither affects the measured inference algorithm. The frozen manifest and its measurement-time hash are retained unchanged; this note corrects the metadata claim.
