# Open-detector proxy diagnostic 2

Frozen 2026-09-10 before any local inference output was seen. This protocol and the inference manifest extend the original immutable corpus freeze with two human controls; they do not replace it.

Question: do pinned Oculus v2 and Greyscope v2 outputs offer useful cheaper feedback on the direction of our historical GPTZero changes?

Use all 10 selected paired comparisons (16 unique archived all-prose editor inputs) and exactly two independently selected historical human paragraphs. Preserve original bytes, including final newlines. The original corpus spans three recurring topics and includes shared baselines and two related business reductions; it is not 10 independent cases. It is enriched for known successes and ties, not an unseen benchmark. Do not access sealed confirmation cases.

Primary local measurements are Oculus sigmoid AI probability and Greyscope calibrated AI involvement, on their native 0–1 scales. They have different meanings and must not be presented as GPTZero confidence. Retain all native outputs, preprocessing, hashes, versions, elapsed time, and failures. No silent truncation: reject any over-limit full model input. For Greyscope preserve the original bytes, author-cleaned text and full calibrated prompt separately.

Before opening scores, define a meaningful local reduction as baseline minus candidate >= 0.01. Define a historical GPTZero reduction as >= 5 percentage points. Report the exact deltas too, and zero-threshold directional signs as a sensitivity description, without selecting another threshold after seeing results. Count recognized historical reductions and misleading local reductions on historical ties. Show every pair and distinguish quotation from rewriting, repeated business-topic observations, and technical cases. Do not claim independent accuracy, fit a calibration model, or generalize an aggregate rate from this sample.

The two pre-1920 human controls are sanity checks only. They are out of domain, may appear in training, and cannot estimate a modern human false-positive rate. There are no GPTZero scores for them.

Use pinned local weights with publisher inference and decoding, strict file/hash checks, recorded input token counts, and one publisher-entrypoint parity check per model where practical. Run models sequentially on the Mac. Obtain an independent review of source correspondence, inference semantics, and final arithmetic; disclose the reviewer's prior survey context.

No new commercial scans, paid compute, purchases, public comments, or candidate rewrites are part of this diagnostic. A useful result can justify a later prospective validation; it cannot itself promote a writing skill, establish a new GPTZero improvement, or replace the standing scan requirement.
