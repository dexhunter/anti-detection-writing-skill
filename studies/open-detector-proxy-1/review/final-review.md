# Final independent review

PASS for the saved runtime evidence and arithmetic. The review independently checked 446 conditions, including all model and input hashes, every submitted token sequence, complete raw-to-cleaned-to-prompt coverage, native score reconstruction and every comparison in `analysis.json`. Both models completed all 18 inputs. There were no missing records, over-limit inputs, silent truncations or content-removal events in the actual batch.

Oculus uses the published masked-mean single-logit classifier and sigmoid. Its publisher-class CPU replay loaded all weights cleanly and reproduced the measured MPS result on the first input within 0.000000858 probability, below the frozen 0.0001 tolerance. Greyscope follows the author's preprocessing, prompt, MLX model and calibrated CORN decoder. The publisher entrypoint replay produced identical token IDs, logits and native output on the first input. Greyscope's six saved tests cover decoder math, threshold boundaries, invalid logits, byte retention, content-removal flags and the no-truncation boundary. The replay checks each cover one input; Greyscope shares the loaded model, so this is entrypoint correspondence rather than an independently implemented model forward pass.

At the frozen 0.01 local-drop threshold, Oculus recognizes 2 of the 3 historical GPTZero reductions and suggests misleading reductions on 4 of the 7 ties. Greyscope recognizes 3 of 3 and suggests misleading reductions on 2 of 7. Under the prespecified zero-drop sensitivity, the respective counts are 3/3 with 4/7 misleading reductions for Oculus and 3/3 with 3/7 for Greyscope. These counts match independent recomputation from the raw logits and archived observations.

The strongest counterexample must remain prominent: Greyscope falls from 0.475194 to 0.284564 on the r20 technical rewrite, while GPTZero stays at 100 percent AI. Its two large matching business reductions, 0.391851 to 0.002059 and 0.365295 to 0.003131, reuse the same exposed Atlas case. The third match adds a quotation rather than rewriting the body. These are selected, dependent observations, not an accuracy estimate or evidence of a general selection rule.

The supported conclusion is that Greyscope is the more promising diagnostic within this batch. Neither model is an adopted selector, GPTZero replacement, new GPTZero improvement or validated writing-skill improvement. The two familiar historical human controls are out-of-domain sanity checks only. All model scores retain their distinct native meanings.

The metadata erratum correctly fixes the raw-README formatting claim while preserving the measured manifest. The earlier qualification on crossover service word counts remains. One analyzer limitation matters for future runs: it counts historical reduction/tie denominators among available pairs. All ten actual pairs are available, so this report is correct; future failures must retain the original denominators or clearly distinguish available-only counts.

This is an independent source, runtime-artifact and arithmetic audit using prior survey/research context, not a fresh blind writing-quality review. The reviewer performed offline tokenization and arithmetic, but no model inference, private holdout access, browser scan or purchase. Public report wording itself was not yet reviewed.
